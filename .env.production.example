# Production: backend public URL (no trailing slash)
# Example (Railway):
VITE_API_URL=https://attendencebackend-production.up.railway.app

# Optional: must match Backend API_KEY if that is set
# VITE_API_KEY=
